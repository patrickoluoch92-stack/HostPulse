{
  "name": "apps",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps",
  "projectType": "application",
  "tags": [],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps",
        "command": "npx next dev --hostname 0.0.0.0 --port 4200",
        "env": {
          "HOST": "0.0.0.0",
          "PORT": "4200"
        }
      },
      "configurations": {}
    }
  }
}
